# Fixture vault — authoring notes

If you're reading this inside `src/content/`, something has gone wrong. **This file should never sync.** It exists to verify that the sync script's skip rule (R-F-05) drops `CLAUDE.md` files at any depth in the source tree.

See `_README.md` in this same folder for what's actually going on here.
