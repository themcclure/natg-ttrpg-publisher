{
  "name": "natg-publisher",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node --env-file-if-exists=.env scripts/verify-content.ts && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "sync": "node --env-file-if-exists=.env scripts/sync.ts",
    "verify": "node --env-file-if-exists=.env scripts/verify-content.ts"
  },
  "dependencies": {
    "astro": "^6.3.1",
    "gray-matter": "^4.0.3",
    "unist-util-visit": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@fontsource-variable/inter": "^5.2.8",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
